/** @type {import('next').NextConfig} */
// Plataforma de estudos em Next.js (App Router). Deploy na Vercel.
// O conteúdo é compilado do curso (../modulos) por scripts/sync-conteudo.mjs.
const nextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
