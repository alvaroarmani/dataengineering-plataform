# Copie para web/.env.local (não versionado) e preencha. Também configure na Vercel.
# A anon key é PÚBLICA por design (protegida por RLS). Ver web/supabase/README.md.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
