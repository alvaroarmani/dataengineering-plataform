curso:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'postgres') }}"
      port: 5432
      user: "{{ env_var('POSTGRES_USER', 'curso') }}"
      password: "{{ env_var('POSTGRES_PASSWORD', 'curso') }}"
      dbname: "{{ env_var('POSTGRES_DB', 'curso') }}"
      schema: public
      threads: 4
