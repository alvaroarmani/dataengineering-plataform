# Build da plataforma (Jupyter Book) — versões pinadas para reprodutibilidade.
# Instale com: pip install -r requirements.txt
jupyter-book==1.0.3
jupyterlite-sphinx==0.16.5
jupyterlite-pyodide-kernel==0.4.7
sphinx-design==0.6.1
sphinxcontrib-mermaid==0.9.2

# Bibliotecas usadas nos labs/exercícios locais (bancada Docker também as instala).
pandas==2.2.3
numpy==2.1.3
duckdb==1.1.3
matplotlib==3.9.2
pyarrow==18.1.0
requests==2.32.3

# Testes (padrão de correção "faça o pytest passar")
pytest==8.3.4
# Graders reais (track ferramentas): conectam no Postgres da bancada. Sem ele, os graders fazem skip.
psycopg2-binary==2.9.12

# Usado pelo linter de conteúdo (scripts/verificar-conteudo.py); também dep transitiva do Jupyter Book.
PyYAML==6.0.2
