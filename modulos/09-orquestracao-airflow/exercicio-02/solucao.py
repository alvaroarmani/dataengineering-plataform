"""Exercício 02 (M9) — Uma DAG precisa ser ACÍCLICA. Detecte ciclos.

Se houver um ciclo (a depende de b que depende de a), não é uma DAG válida — o pipeline
nunca terminaria. Implemente a detecção e rode `pytest -q`.
"""


def tem_ciclo(tasks, arestas):
    """`arestas` = lista de (a, b) ("b depende de a"). Retorne True se o grafo tiver ciclo,
    False se for acíclico (uma DAG válida)."""
    # SEU CÓDIGO AQUI
    raise NotImplementedError
