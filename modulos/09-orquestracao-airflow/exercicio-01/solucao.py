"""Exercício 01 (M9) — Ordem topológica: em que ordem o Airflow roda as tasks.

Dado o grafo (tasks + dependências), devolva uma ordem que respeite todas as dependências
(cada task só aparece depois das que ela depende). Rode `pytest -q`.
"""


def ordem_topologica(tasks, arestas):
    """`tasks` = lista de nomes; `arestas` = lista de (a, b) significando 'b depende de a'
    (a roda antes de b). Retorne uma lista com TODAS as tasks numa ordem válida (topológica)."""
    # SEU CÓDIGO AQUI
    raise NotImplementedError
