"""Exercício 08 (M9) — Estado de um DAG run (observabilidade).

Resuma o resultado de uma execução a partir do status das tasks. Rode `pytest -q`.
"""


def estado_do_run(tasks):
    """`tasks` = lista de (nome, status), status em {'success','failed','skipped'}.
    Retorne um dict com as contagens e se o run passou:
      {'success': n, 'failed': n, 'skipped': n, 'run_ok': bool}
    `run_ok` é True somente se NENHUMA task falhou."""
    # SEU CÓDIGO AQUI
    raise NotImplementedError
