"""Exercício 04 (M3) — Receita por categoria com pandas.

Implemente `receita_por_categoria` e rode `pytest -q` nesta pasta até tudo passar.
"""


def receita_por_categoria(df):
    """Recebe um DataFrame com colunas 'categoria' e 'valor' (pode ter NaN).

    Retorna um DataFrame com colunas 'categoria' e 'receita' (soma por categoria),
    ignorando valores nulos, ordenado por receita decrescente e com índice reiniciado.
    """
    # SEU CÓDIGO AQUI
    raise NotImplementedError
