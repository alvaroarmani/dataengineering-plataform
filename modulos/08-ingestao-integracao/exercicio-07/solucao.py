"""Exercício 07 (M8) — Validar um registro contra um schema (a ideia do Avro/Parquet).

Formatos com schema (Avro, Parquet) rejeitam dados fora do tipo. Implemente a validação e
rode `pytest -q`.
"""


def valida_registro(registro, schema):
    """`schema` = dict {campo: tipo_python} (ex.: {'id': int, 'nome': str}).
    Retorne a lista ORDENADA dos campos com PROBLEMA: ausentes no registro OU com tipo errado.
    Registro válido => lista vazia."""
    # SEU CÓDIGO AQUI
    raise NotImplementedError
