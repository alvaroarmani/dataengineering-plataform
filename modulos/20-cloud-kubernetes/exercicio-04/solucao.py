"""Exercício 04 (M20) — Autoscaling (HPA). Implemente e rode `pytest -q`."""


def hpa_replicas(replicas_atuais, cpu_atual, cpu_alvo):
    """O HorizontalPodAutoscaler ajusta réplicas pela fórmula: desejado = ceil(replicas_atuais * cpu_atual / cpu_alvo). Retorne o número desejado de réplicas."""
    # SEU CÓDIGO AQUI
    raise NotImplementedError
