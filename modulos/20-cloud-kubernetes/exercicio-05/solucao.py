"""Exercício 05 (M20) — Cabe no nó? (scheduler). Implemente e rode `pytest -q`."""


def cabe_no_no(pods_cpu, cpu_no):
    """pods_cpu = lista de CPU requisitada por cada pod. Retorne True se a soma das requisições couber na capacidade `cpu_no` do nó."""
    # SEU CÓDIGO AQUI
    raise NotImplementedError
