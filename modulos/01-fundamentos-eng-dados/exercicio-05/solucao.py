"""Exercicio 05 (M1) - Vantagem do colunar (bytes lidos). Implemente e rode `pytest -q`."""


def bytes_colunar(total_colunas, lidas, bytes_por_coluna):
    """No armazenamento colunar, a consulta le SO as colunas necessarias. Retorne os bytes lidos = lidas * bytes_por_coluna (independente do total de colunas)."""
    # SEU CODIGO AQUI
    raise NotImplementedError
