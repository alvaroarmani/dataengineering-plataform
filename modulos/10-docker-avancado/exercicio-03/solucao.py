"""Exercício 03 (M10) — Ordem de subida (depends_on). Implemente e rode `pytest -q`."""


def ordem_subida(servicos, depends_on):
    """Retorne a ordem de subida (topológica) respeitando depends_on = lista de (a, b) onde 'b depende de a' (a sobe antes)."""
    # SEU CÓDIGO AQUI
    raise NotImplementedError
