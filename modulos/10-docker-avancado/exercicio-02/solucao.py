"""Exercício 02 (M10) — .dockerignore: contexto de build. Implemente e rode `pytest -q`."""


def enviados_ao_contexto(arquivos, ignore):
    """Retorne (ordenados) os arquivos que VÃO para o contexto (não casam com nenhum padrão de ignore). Padrão terminado em '/' ignora por prefixo; senão, por igualdade."""
    # SEU CÓDIGO AQUI
    raise NotImplementedError
