"""Exercício 01 — O que o .gitignore ignora.

Implemente `arquivos_ignorados` e rode `pytest -q` nesta pasta até tudo passar.
"""


def arquivos_ignorados(arquivos, padroes):
    """Recebe uma lista de caminhos e padrões simplificados de .gitignore.

    Retorna os caminhos ignorados, na ordem de entrada. Padrões suportados:
      - nome exato:        ".env"
      - curinga de extensão: "*.log"
      - pasta (prefixo):   "data/"
    """
    # SEU CÓDIGO AQUI
    raise NotImplementedError
