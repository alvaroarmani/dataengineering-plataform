"""Exercício 04 (M18) — Chave de partição (wide-column). Implemente e rode `pytest -q`."""


def chave_particao(linha, cols):
    """No modelo wide-column (Cassandra), a chave de partição é composta pelas colunas de particionamento. Retorne a TUPLA dos valores de `linha` nas colunas `cols`, nessa ordem."""
    # SEU CÓDIGO AQUI
    raise NotImplementedError
