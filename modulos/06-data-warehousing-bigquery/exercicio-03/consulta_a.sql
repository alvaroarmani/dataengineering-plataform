-- A) Receita MENSAL de 2025, sobre a tabela PARTICIONADA `pedidos(ano, mes, categoria, valor)`.
-- Colunas: (mes, receita) = SUM(valor), ordenado por mes crescente.
-- Filtre pela coluna de partição (ano = 2025) para o Postgres podar as outras partições.

-- SEU CÓDIGO AQUI
