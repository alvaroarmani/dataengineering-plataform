"""Exercício 06 (M17) — Exactly-once por dedup (idempotência). Implemente e rode `pytest -q`."""


def processar_idempotente(eventos):
    """eventos = lista de (id, valor), podendo repetir id (reentrega). Processe cada id UMA vez (a 1ª ocorrência vence) e retorne a soma dos valores — reprocessar não infla o total."""
    # SEU CÓDIGO AQUI
    raise NotImplementedError
